name: Auto Build function (Use for call only)
on:
 workflow_call:
    inputs:
      arch:
        type: string
      gapps:
        type: string
      root:
        type: string
      insider:
        type: string
jobs:
  build:
    name: ${{ inputs.arch }}_${{ inputs.root }}_${{ inputs.gapps }} (Insider is ${{ inputs.indider }})
    runs-on: ubuntu-latest
    steps:
      - name: Checkout ♻️
        uses: actions/checkout@v4

      - name: Setup Python 🏭
        uses: actions/setup-python@v4
        with:
          check-latest: true
          python-version: '3.x'
          cache: 'pip'
          cache-dependency-path: scripts/

      - name: Setup Python3 Virtual Enviroment 🐍
        working-directory: scripts
        run: |
          sudo apt-get update
          PYTHON_VENV_DIR="$(dirname "$PWD")/python3-env"
          python3 -m venv "$PYTHON_VENV_DIR" || abort "Failed to create python3 virtual env"
          # shellcheck disable=SC1091
          source "$PYTHON_VENV_DIR/bin/activate" || abort "Failed to activate python3 virtual env"
          python3 -c "import pkg_resources; pkg_resources.require(open('requirements.txt',mode='r'))" &>/dev/null || {
              echo "Installing Python3 dependencies"
              python3 -m pip install --upgrade -r requirements.txt || abort "Failed to install python3 dependencies"
          }
          deactivate

      - name: Install Ubuntu Dependencies 🧑‍🏭
        uses: awalsh128/cache-apt-pkgs-action@v1
        with:
          packages: e2fsprogs attr unzip qemu-utils python3-venv
          version: 1.0

      - name: Build WSA ${{ inputs.arch }} with GApps ${{ inputs.root }} 🏗️
        id: wsa
        run: |
          if [[ -z "${{ inputs.insider }}" ]]; then
              RELEASE_TYPE="retail"
          else
              RELEASE_TYPE="latest"
          fi

          mkdir -p download
          ./scripts/build.sh --arch ${{ inputs.arch }} --release-type $RELEASE_TYPE --magisk-ver stable --gapps-brand ${{ inputs.gapps }} --root-sol ${{ inputs.root }} --custom-model redfin --remove-amazon --compress-format zip

      - name: Pass to Windows 💸
        uses: actions/cache/save@v3
        with:
          path: output
          key: ${{ steps.wsa.outputs.artifact }}-${{ steps.wsa.outputs.date }}
          enableCrossOsArchive: true

      - name: Prepare release tag 🏷️
        id: date
        run: |
           if [[ -z "${{ inputs.insider }}" ]]; then
              echo "tag=$(date +'v%Y-%m-%d')" >> $GITHUB_OUTPUT
           else
              echo "tag=WSA-Insider-Version" >> $GITHUB_OUTPUT
           fi

    outputs:
      zipname: ${{ steps.wsa.outputs.artifact }}
      date: ${{ steps.wsa.outputs.date }}
      tag: ${{ steps.date.outputs.tag }}

  make-pri:
    name: Merge PRI resources
    runs-on: windows-latest
    needs: build
    steps:
      - name: Checkout ♻️
        uses: actions/checkout@v4

      - name: Download built artifact ⌛
        uses: actions/cache/restore@v3
        with:
          path: output
          key: ${{ needs.build.outputs.artifact }}-${{ needs.build.outputs.date }}
          enableCrossOsArchive: true
          fail-on-cache-miss: true

      - name: Merge language and density resources 🧙🏻‍♂️
        run: (Start-Process pwsh.exe -NoNewWindow -PassThru -Args "-ExecutionPolicy Bypass -File MakePri.ps1" -WorkingDirectory "${{ github.workspace }}\output\${{ needs.build.outputs.zipname }}").WaitForExit()

      - name: Compact Images 💿
        run: |
         foreach ($Partition in 'system','product','system_ext','vendor') {
             Write-Output "Optimizing of $Partition..."
             Write-Output "SELECT VDISK FILE=`"${{ github.workspace }}\output\${{ needs.build.outputs.zipname }}\$Partition.vhdx`"`
             ATTACH VDISK READONLY`
             COMPACT VDISK`
             DETACH VDISK" | Set-Content -Path "$Partition.txt" -Encoding Ascii
             Start-Process -NoNewWindow -Wait "diskpart.exe" -Args "/s $Partition.txt" -RedirectStandardOutput NUL
         }

      - name: Remove unused artifact ❌
        uses: geekyeggo/delete-artifact@v2
        with:
          name: ${{ needs.build.outputs.zipname }}

      - name: Compress with 7-Zip and Set checksum 📦
        id: run
        run: |
          7z a -t7z -mx=6 -m0=LZMA2 -ms=on -mmt=8 -sdel -- ${{ needs.build.outputs.zipname }}.7z .\output\${{ needs.build.outputs.zipname }}\*
          ("hash=" + $(Get-FileHash -Path ${{ needs.build.outputs.zipname }}.7z -Algorithm SHA256).Hash.ToLower().ToString()) | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding UTF8 -Append

      - name: Update build to release 🤌
        uses: softprops/action-gh-release@v1
        with:
          files: ${{ needs.build.outputs.zipname }}.7z
          fail_on_unmatched_files: true
          append_body: true
          tag_name: ${{ needs.build.outputs.tag }}
          body: ${{ steps.run.outputs.hash }}  ${{ needs.build.outputs.zipname }}.7z
